<div class="row">
  <div class="container">
    
      <div class="show-span first-show">
        <h3 class="dark show top-show"><%= @tutorial.title %> <small>| <%= @tutorial.length %> minute screencast | <% if @tutorial.package.title == "Introduction to Excel" %>
                <%= "Beginner" %>
                <% else %>
                <%= "Intermediate" %>
                <% end %> </small></h3>
          <div id="myElement">Loading the player...</div>
            <script type="text/javascript">
                $(document).ready(function(){
                  jwplayer("myElement").setup({
                      height: 525,
                      width: 900,
                      file: '<%= @tutorial.video_path %>',
                      volume: 75,
                      controlbar: "bottom",
                  });              
                });
            </script>
          <div class="tutorial-show-span-pull-left">
            <h3 class="dark show below-video">Series
              <% if @tutorial.package.id == 2 %> 
                  #<%= @tutorial.id - @tutorial.package.tutorials.count %>
                <% else %> 
                  #<%= @tutorial.id %>
              <% end %> - <%= @tutorial.package.title %></h3>
          </div>
          <div class="download-span-pull-right">      
            
          </div>  
        </div>        
        <div class="show-span">
          <div class="description-show-span-pull-left">
            <h3>Description</h3>
            <p><%= @tutorial.description %></p>
            <p>Download the Excel file used in this episode for free.</p>
            <%= link_to "Excel Tutorial File <i class='icon-cloud-download'></i>".html_safe, "#{@tutorial.excel_path}", :target => "_blank", :class=>"btn btn-primary" %> 
          </div>  
          <div class="topics-span-pull-right">      
            <h3>Topics Covered</h3>
            <ul class="topic-list">
              <% @tutorial.topics.each do |topic| %>
              <li class="course-summary-topics"><%= link_to topic.title, topic_url(topic.id) %></li>
              <% end %>
            </ul>
          </div>  
        </div>
      </div>
      </div>

      <div class="row">
        <div class="container">
          <div class="show-span">
            <h3>Skill Assessment</h3>
            <p>After mastering each of the topics in the <%= @tutorial.title %> series, try putting your Excel knoweldge to the test!</p>

            <p>Simply download our Skill Assessment, then upload your answer file.  We will review and let you know if your Skill Assessment needs improvement or if you earned the certificate of completion.</p>

            <p>Manage all Skill Assessments through your <%= link_to "Dashboard", user_url %> after signing in.</p>

            <h4 class="skill-assessment-download-heading">Download, Open and Follow the Directions</h4>
            <% if !@tutorial.blank_quizzes.first.nil? %>
              <%= link_to "Skill Assessment <i class='icon-cloud-download'></i>".html_safe, "#{@tutorial.tutorial.excel_path}", :target => "_blank", :class=>"btn btn-primary" %>
            <% else %>
              <p style="color: green;">We'll get a Skill Assessment posted soon for this topic!  In the meantime, please let us know if you want one by leaving a comment below or giving feedback on the left.</p>
            <% end %>

          </div>
        </div>
      </div>        


<div class="row">
  <div class="container">
    <div class="span11">
      <h3>Leave a Comment</h3>
      <div class="span11 disqus-container">
        <%= render "layouts/comments" %>
      </div>
  </div>
  </div>
</div>